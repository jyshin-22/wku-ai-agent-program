# CLAUDE.md (예시)

> 이 파일은 프로젝트 루트(`./CLAUDE.md`)에 두면 매 세션 자동으로 읽힙니다.
> `/init` 로 생성한 뒤, 아래처럼 프로젝트 규칙을 채우세요. (예시이므로 자유롭게 수정)

## 프로젝트 개요
- 무엇: [예) 개인 미니 프로젝트 — CSV 가계부 요약기]
- 언어/도구: Python (표준 라이브러리 우선)

## 작업 규칙
- 코드를 만든 뒤 **직접 실행해서 동작을 확인**한다.
- 한 번에 큰 변경 대신, 작은 단위로 만들고 확인한다.
- 외부 라이브러리는 꼭 필요할 때만, 설치 방법을 함께 안내한다.
- 파일을 지우거나 덮어쓰기 전에 한 번 확인한다.

## 출력/스타일
- 설명은 한국어로 간결하게.
- 터미널 명령은 복사할 수 있게 코드 블록으로.

## 하지 말 것
- 근거 없는 수치/출처를 지어내지 않는다(모르면 모른다고).
- 시스템 전체에 영향을 주는 명령(rm -rf 등)은 실행하지 않는다.
