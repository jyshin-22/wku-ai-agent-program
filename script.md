# 강의 대본 (강사 전용) — AI 에이전트와 응용

> 슬라이드와 1:1 대응. 각 항목: **화면**(슬라이드 요점) + **대본/진행**(말할 내용).
> 자동 생성물입니다 — 슬라이드 변경 후 `python3 build_script.py` 로 갱신.
> 대본은 `script_notes.py` 에서 보강. `[보강 예정]` = 아직 미작성(요점만).

---

## Day 1 (월) — LLM의 원리와 이해

### 오후 특강 2 · LLM의 원리와 이해  (90분 · 14:30–16:00)

*세션 목표: LLM이 무엇을 어떻게 하는지 — 토큰 예측·학습·프롬프트·한계를 이해한다.*

#### 1. 이 세션에서 다룰 것

**화면**
- **LLM의 작동 원리** — 토큰 예측 · 학습 · 추론
- **외부 지식과 결합** — 임베딩·의미 검색·RAG → 리서치 에이전트의 토대
- **제어와 한계** — 프롬프트 · 모델 선택 · 환각 등 주의점

**대본/진행**

오늘은 에이전트를 만들기 전에, 그 두뇌인 LLM이 실제로 무엇을 하는지부터 봅니다. 핵심은 의외로 단순해요 — '다음에 올 단어 조각을 확률로 예측'하는 것 하나입니다. 여기에 더해, 모델 혼자서는 부족하기 때문에 외부 지식을 붙이는 임베딩과 RAG를 배웁니다. 이게 이번 주에 만들 리서치 에이전트의 토대예요. 마지막으로 프롬프트로 동작을 바꾸는 법과, 환각 같은 한계를 구분하는 눈을 기릅니다.

#### 2. LLM이란 무엇인가

**화면**
- **Large Language Model** — 방대한 텍스트로 학습한 대규모 신경망 — 언어를 생성·처리
- **핵심 동작** — 지금까지의 텍스트로 '다음에 올 토큰'을 확률로 예측
- **'이해'가 아니라 '패턴'** — 의미를 아는 게 아니라 통계적 패턴을 재현하는 것

**대본/진행**

LLM은 인터넷 규모의 텍스트로 학습한 거대한 확률 모델입니다. 우리는 '이해한다'고 느끼지만, 내부에서는 '이 문맥 다음에 어떤 토큰이 올 확률이 높은가'를 계산할 뿐이에요. 비유하면, 수많은 문장을 읽은 사람이 빈칸 뒤에 올 말을 '아마 이거겠지' 하고 채우는 것과 같습니다. 의미를 진짜로 아는 게 아니라 통계적 패턴을 재현한다는 점 — 이게 뒤에 나올 환각의 뿌리입니다.

#### 3. 입력에서 출력까지

**화면**
- 토큰화(문장을 토큰(단어 조각)으로 분해) → 예측(다음 토큰의 확률 분포 계산) → 샘플링(확률에 따라 토큰 1개 선택) → 반복(붙이고 다시 예측 — 끝까지)

**대본/진행**

동작을 한 줄로 보면 이렇습니다. 먼저 문장을 토큰이라는 작은 조각으로 쪼개고, 각 시점에서 다음 토큰의 확률 분포를 계산해, 그중 하나를 골라 붙입니다. 그리고 이 과정을 끝날 때까지 반복하죠. 중요한 건 '한 번에 한 토큰씩'이라는 점입니다 — 그래서 긴 답일수록 앞에서의 작은 흔들림이 누적될 수 있어요. 이 자기회귀(autoregressive) 구조를 알면 temperature나 환각이 왜 생기는지 자연스럽게 이해됩니다.

#### 4. 사전학습 (Pre-training)

**화면**
- **방대한 텍스트로 자기지도 학습** — 웹·책·코드에서 '다음 토큰 맞히기'를 반복
- **무엇을 배우나** — 문법·세상 지식·추론 패턴·문체까지 광범위하게
- **한계의 씨앗** — 학습 시점 이후 정보는 모름 · 데이터 편향을 흡수

**대본/진행**

모델은 두 단계로 만들어집니다. 첫 단계가 사전학습으로, 웹·책·코드 같은 방대한 텍스트에서 '다음 토큰 맞히기'를 끝없이 반복해요. 이 과정에서 문법뿐 아니라 세상 지식, 추론 패턴, 문체까지 폭넓게 흡수합니다. 단, 학습한 시점 이후의 정보는 모르고, 데이터에 있던 편향도 그대로 들어옵니다. 예를 들어 작년에 학습한 모델은 올해 새 논문을 모르죠 — 그래서 나중에 검색·RAG가 필요해집니다.

#### 5. 정렬 (Alignment)

**화면**
- **사전학습만으론 부족** — '유용하고·안전하게·지시를 따르도록' 추가로 다듬음
- **instruction tuning** — 지시–응답 예시로 '시키는 대로' 하도록 미세조정
- **인간 피드백 (RLHF 등)** — 사람이 선호하는 답을 강화 — 도움되고 해롭지 않게

**대본/진행**

사전학습만 끝난 모델은 똑똑하지만 말을 잘 안 듣습니다. 그래서 두 번째 단계인 정렬에서, 사람이 원하는 방식으로 — 지시를 따르고, 안전하고, 도움이 되게 — 다듬습니다. 대표적으로 '지시-응답' 예시로 미세조정하는 instruction tuning, 그리고 사람이 선호한 답에 더 높은 점수를 주는 인간 피드백(RLHF 같은) 방식이 있어요. 우리가 ChatGPT나 Claude에서 느끼는 '말귀를 알아듣는' 느낌이 바로 이 정렬의 결과입니다.

#### 6. 추론(사고)하는 모델

**화면**
- **일반 생성 vs 추론** — 바로 답하지 않고, 단계적으로 '생각'한 뒤 답한다
- **언제 강한가** — 복잡한 문제 · 다단계 추론 · 코딩
- **트레이드오프** — 더 정확하지만 더 느리고 비싸다

**대본/진행**

최근에는 바로 답하지 않고, 속으로 단계를 밟아 '생각'한 뒤 답하는 추론 모델이 나왔습니다. 복잡한 수학, 다단계 논리, 코딩처럼 한 번에 답하기 어려운 문제에서 특히 강해요. 대신 생각하는 시간과 토큰을 더 쓰기 때문에 느리고 비쌉니다. 그래서 모든 작업에 추론 모델을 쓰는 게 아니라, 어려운 문제에만 골라 쓰는 판단이 필요합니다 — 뒤의 '모델 선택'과 연결됩니다.

#### 7. 임베딩 & 의미 검색

**화면**
- **임베딩이란** — 텍스트를 의미를 담은 숫자 벡터로 변환
- **의미 검색** — 글자가 달라도 '뜻이 비슷한' 문서를 찾음 (키워드 매칭의 한계 극복)
- **어디에 쓰나** — 방대한 문헌에서 관련 자료 검색 — 리서치 에이전트의 토대

**대본/진행**

이제 외부 지식을 붙이는 첫 조각, 임베딩입니다. 임베딩은 텍스트를 '의미를 담은 숫자 벡터'로 바꾸는 기술이에요. 그러면 글자가 달라도 뜻이 가까운 문장끼리는 벡터도 가까워집니다. 예를 들어 '심장마비'와 '심근경색'은 글자는 다르지만 의미 검색에서는 비슷한 것으로 잡혀요. 키워드가 정확히 일치하지 않아도 관련 자료를 찾을 수 있다는 것 — 이게 방대한 문헌을 뒤지는 리서치 에이전트의 핵심 도구입니다.

#### 8. 검색 증강 생성 (RAG)

**화면**
- 질문(사용자 질문) → 검색(관련 문서 찾기(임베딩)) → 주입(찾은 근거를 프롬프트에) → 생성(근거 기반으로 답)

**대본/진행**

임베딩으로 찾은 자료를 LLM에 '먹여서' 답하게 하는 게 RAG입니다. 흐름은 간단해요: 질문이 들어오면, 관련 문서를 검색하고, 그 근거를 프롬프트에 넣은 다음, 모델이 그 근거를 바탕으로 답합니다. 이렇게 하면 두 가지가 좋아집니다 — 모델이 모르던 최신·전문 정보를 답할 수 있고, 근거가 있으니 환각이 줄어듭니다. 이번 주 팀 프로젝트가 바로 이 RAG 패턴의 미니 버전이라고 보면 됩니다.

#### 9. 프롬프트가 동작을 바꾼다

**화면**
- **프롬프트 = 모델에 주는 입력 전체** — 지시 + 맥락 + 예시 + 데이터
- **좋은 프롬프트** — 명확한 목표 · 충분한 맥락 · 원하는 형식 명시
- **나쁜 프롬프트** — 모호·맥락 부족 → 엉뚱하거나 일반적인 답

**대본/진행**

같은 모델이라도 프롬프트를 어떻게 주느냐에 따라 결과가 완전히 달라집니다. 프롬프트는 단순한 질문이 아니라 '지시 + 맥락 + 예시 + 데이터'를 합친 입력 전체예요. 좋은 프롬프트는 목표가 분명하고, 충분한 맥락을 주고, 원하는 출력 형식을 명시합니다. 반대로 모호하게 던지면 모델은 가장 무난하고 일반적인 답을 내놓죠. '코딩 도와줘'와 '이 CSV를 항목별로 합산하는 파이썬 코드를 만들고 실행해서 확인해줘'의 차이를 떠올리면 됩니다.

#### 10. 자주 쓰는 패턴

**화면**
- **역할·맥락 부여** — '너는 ~전문가' + 배경 → 답의 톤·깊이 조정
- **예시 제공 (few-shot)** — 원하는 입출력 예 2~3개 → 형식이 안정
- **단계적 사고 (CoT)** — '단계별로 생각해줘' → 복잡한 추론 정확도↑

**대본/진행**

실무에서 자주 쓰는 프롬프트 기법 세 가지를 소개합니다. 첫째, 역할과 맥락 부여 — '너는 약학 전문가야' 같은 한 줄이 답의 깊이와 톤을 바꿉니다. 둘째, 예시 제공(few-shot) — 원하는 입력·출력 예를 두세 개 보여주면 형식이 안정됩니다. 셋째, 단계적 사고(chain-of-thought) — '단계별로 생각해줘'라고 하면 복잡한 추론의 정확도가 올라가요. 이 셋만 익혀도 결과 품질이 눈에 띄게 달라집니다.

#### 11. 같은 모델, 다른 출력

**화면**
- **temperature** — 낮으면 결정적·일관, 높으면 다양·창의(불안정)
- **확률적 생성** — 같은 질문도 매번 다른 답이 나올 수 있다
- **재현성에의 함의** — 리서치엔 낮은 temperature가 유리 — 결과 안정

**대본/진행**

같은 질문을 줬는데 답이 매번 다른 경험, 해보셨을 겁니다. 이건 버그가 아니라 확률적 생성의 본질이에요. temperature라는 값이 이 다양성을 조절하는데, 낮으면 매번 비슷하고 안정적인 답을, 높으면 다양하지만 불안정한 답을 줍니다. 브레인스토밍에는 높게, 사실 기반 리서치에는 낮게 두는 게 좋아요. 특히 리서치에서는 재현성이 중요하니 temperature를 낮춰 결과를 안정시키는 걸 권합니다.

#### 12. 컨텍스트 윈도우

**화면**
- **한 번에 볼 수 있는 토큰 한계** — 프롬프트+대화+자료가 이 한도 안에 들어가야
- **길어지면** — 비용↑·속도↓·중간 내용 놓침(lost in the middle)
- **에이전트와의 관련** — 외부 메모리·검색·도구로 한계를 보완

**대본/진행**

모델이 '한 번에 볼 수 있는 정보의 양'에는 한계가 있는데, 이걸 컨텍스트 윈도우라고 합니다. 프롬프트, 지금까지의 대화, 첨부한 자료가 모두 이 한도 안에 들어가야 해요. 너무 길어지면 비용과 속도가 나빠지고, 가운데 있는 내용을 놓치는 현상(lost in the middle)도 생깁니다. 그래서 에이전트는 모든 걸 컨텍스트에 욱여넣는 대신, 외부 메모리와 검색으로 '필요할 때 필요한 것만' 가져오도록 설계합니다.

#### 13. LLM의 능력과 한계

**화면**
- 잘하는 것: 요약·번역·분류 · 코드 생성·설명 · 텍스트 추출·재구성 · 브레인스토밍
- 주의할 것: 환각 — 없는 사실 지어냄 · 최신·실시간 정보 · 정확한 계산·셈 · 일관된 장기 추론

**대본/진행**

여기서 균형 잡힌 시각이 중요합니다. LLM은 요약·번역·분류, 코드 생성, 텍스트 추출·재구성, 브레인스토밍에 아주 강해요. 반대로 없는 사실을 지어내는 환각, 최신·실시간 정보, 정확한 계산이나 셈, 아주 긴 추론의 일관성에는 약합니다. 핵심 메시지는 'LLM을 만능으로 믿지 말고, 강점은 활용하고 약점은 도구로 보완하라'는 거예요. 다음 슬라이드의 환각이 그 약점의 대표 선수입니다.

#### 14. 환각(hallucination) 바로 알기

**화면**
- **그럴듯하지만 틀린 출력** — 출처·인용·수치를 자신있게 지어내기도
- **왜 생기나** — '그럴듯한 다음 토큰'을 생성할 뿐 사실 검증을 안 함
- **대응** — 출처 확인·검색 연동(RAG)·교차검증 → 리서치 에이전트의 존재 이유

**대본/진행**

환각은 모델이 '그럴듯하지만 틀린' 내용을, 그것도 자신 있게 만들어내는 현상입니다. 가짜 출처나 존재하지 않는 인용, 틀린 수치를 진짜처럼 제시하기도 해요. 왜 그럴까요? 모델은 사실을 검증하는 게 아니라 '그럴듯한 다음 토큰'을 생성할 뿐이기 때문입니다. 그래서 대응은 명확합니다 — 출처를 확인하고, 검색(RAG)으로 근거를 대고, 여러 자료로 교차검증하는 것. 바로 이게 우리가 리서치 에이전트를 만드는 이유입니다.

#### 15. 어떤 모델을 쓸까

**화면**
- **크기 ↔ 성능** — 큰 모델 = 똑똑하지만 느리고 비쌈
- **작업에 맞게** — 간단한 작업엔 작고 빠른 모델, 어려운 작업엔 크고/추론 모델
- **Claude 예** — Opus(최상위) · Sonnet(균형) · Haiku(빠름·저비용)

**대본/진행**

마지막 실용 팁, 모델 선택입니다. 큰 모델일수록 똑똑하지만 느리고 비싸요 — 성능과 비용은 대체로 맞바꿈 관계입니다. 그래서 간단한 작업에는 작고 빠른 모델을, 어렵고 중요한 작업에는 크거나 추론하는 모델을 쓰는 게 합리적입니다. Claude를 예로 들면 Opus는 최상위, Sonnet은 균형, Haiku는 빠른 처리에 적합하죠(구체 모델명은 시점에 따라 바뀌니 확인). 한 작업 안에서도 단계별로 다른 모델을 섞어 쓰는 경우도 많습니다.

#### 16. LLM → 에이전트로 가는 길

**화면**
- **LLM 단독의 한계** — 도구 없음 · 기억 없음 · 한 번의 응답으로 끝
- **RAG·검색을 루프로 = 에이전트** — 찾고 → 검증하고 → 종합 — 리서치 에이전트로
- **다음: Day 2** — 에이전트의 정의와 Claude Code 실습

**대본/진행**

정리하면, LLM은 강력하지만 혼자서는 도구도 기억도 없고 한 번 답하면 끝입니다. 여기에 '도구를 쓰고, 결과를 보고, 다시 시도하는 반복'을 붙인 게 바로 에이전트예요. 특히 오늘 배운 검색·RAG를 루프로 돌리면 — 찾고, 검증하고, 종합하는 — 리서치 에이전트가 됩니다. 즉 오늘의 임베딩과 RAG가 이번 주 프로젝트의 토대입니다. 내일은 이 에이전트의 정의를 자세히 보고, 실습 도구인 Claude Code를 직접 설치합니다.

---

## Day 2 (화) — AI 에이전트 원리 + 클로드 코드

### 오전 특강 · AI 에이전트 원리와 이해 + AI 에이전트 기반 리서치  (90분 · 10:00–11:30)

*세션 목표: 에이전트의 정의·구조·자율성과, 리서치 에이전트의 전체 구조를 이해한다.*

#### 1. 이 세션에서 다룰 것

**화면**
- **에이전트의 정의와 구조** — LLM + 도구(tools) + 반복(loop)
- **바이브코딩 작업 방식** — 자연어로 만들고, 검증하며 반복
- **리서치 에이전트의 큰 틀** — 이번 주 팀 프로젝트의 형태

**대본/진행**

[보강 예정]

#### 2. AI 에이전트란?

**화면**
- **정의** — LLM(두뇌) + 도구(tools) + 반복 루프(loop)로 목표를 수행
- **LLM과의 차이** — 답만 내는 게 아니라 '행동'하고 결과를 보고 다음 행동을 결정
- **예** — 검색 → 읽기 → 코드 실행 → 수정을 스스로 반복

**대본/진행**

[보강 예정]

#### 3. LLM vs 에이전트

**화면**
- LLM 단독: 입력→출력 한 번 · 도구 없음 · 기억 없음 · 사용자가 매번 지시
- 에이전트: 목표를 주면 · 도구를 골라 실행 · 상태·기억 유지 · 스스로 반복·판단

**대본/진행**

[보강 예정]

#### 4. 에이전트의 동작 사이클

**화면**
- 관찰(상황·이전 결과 파악) → 사고(다음에 무엇을 할지 결정) → 행동(도구 호출(검색·실행)) → 관찰(결과 확인 → 반복)

**대본/진행**

[보강 예정]

#### 5. 도구(tools) — 에이전트의 손과 발

**화면**
- **도구란** — 에이전트가 호출하는 외부 기능 — 웹검색·파일·코드실행·API
- **왜 필요한가** — LLM의 한계(최신성·계산·실행)를 실제 행동으로 보완
- **동작 방식** — LLM이 '어떤 도구를 어떤 인자로' 호출할지 결정 → 결과를 다시 입력

**대본/진행**

[보강 예정]

#### 6. 메모리와 컨텍스트

**화면**
- **단기 (컨텍스트)** — 현재 작업 맥락 — 컨텍스트 윈도우 안에 유지
- **장기 (외부 메모리)** — 파일·DB·벡터스토어에 저장해 필요할 때 검색
- **왜 중요한가** — 긴 작업의 일관성 유지 · 맥락 한계 극복

**대본/진행**

[보강 예정]

#### 7. 자율성의 정도

**화면**
- **보조형 (copilot)** — 사람이 매 단계 확인·승인
- **반자율** — 일상 작업은 자동, 중요한 결정만 사람이 확인
- **자율형** — 목표만 주면 스스로 — 강력하지만 검증·안전장치 필수

**대본/진행**

[보강 예정]

#### 8. 바이브코딩 (Vibe Coding)

**화면**
- **정의** — 코드를 직접 타이핑하지 않고, 자연어로 의도를 전달해 만든다
- **사람의 일** — 무엇을·왜를 명확히 + 결과를 검증하고 방향 제시
- **핵심** — 빠른 반복 + 반드시 검증 (출력을 그대로 신뢰 금물)

**대본/진행**

[보강 예정]

#### 9. 바이브코딩 사이클

**화면**
- 의도 전달(원하는 것을 구체적으로) → 생성(에이전트가 구현) → 실행(실제로 돌려 확인) → 검증·수정(결과 보고 다시 지시)

**대본/진행**

[보강 예정]

#### 10. 사람의 역할 변화 · 주의점

**화면**
- **작성자 → 디렉터·검수자** — 타이핑보다 '무엇을 만들지'와 '맞는지' 판단
- **검증을 습관화** — 동작·정확성을 직접 확인 — AI는 틀릴 수 있다
- **오늘 오후 실습에서 체험** — Claude Code로 직접

**대본/진행**

[보강 예정]

#### 11. 리서치 에이전트 개요

**화면**
- 질문(무엇을 알고 싶나 — 질문 분해) → 검색(여러 소스에서 수집) → 검증(출처·신뢰도 교차확인) → 종합(근거를 묶어 답) → 보고(출처와 함께 정리)

**대본/진행**

[보강 예정]

### 오후 특강 1 · Python & Claude Code 설치  (80분 · 13:00–14:20)

*세션 목표: 프로젝트용 Python과 실습 도구 Claude Code를 각자 노트북에 설치하고 동작을 확인한다.*

#### 1. 설치 전 체크리스트

**화면**
- **오늘 설치할 것** — Python (프로젝트용) + Claude Code (도구)
- **대상 OS** — Windows / macOS 만 (Win=PowerShell · Mac=Terminal)
- **계정** — Claude 구독(Pro/Max) 또는 Anthropic Console 계정

**대본/진행**

[보강 예정]

#### 2. python.org 공식 설치 — OS별

**화면**
- Windows: python.org 설치 파일 다운로드 · ★ 'Add python.exe to PATH' 체크 · 확인: python --version · 터미널: PowerShell
- macOS: .pkg 다운로드 → 설치 · ★ 'Install Certificates' 실행 · 확인: python3 --version · 터미널: Terminal(zsh)

**대본/진행**

[보강 예정]

#### 3. 설치 확인 & 트러블슈팅

**화면**
- **확인** — Win: python --version · pip --version  /  Mac: python3 · pip3 --version
- **Windows 문제** — PATH 미설정 → 설치 관리자 'Modify'에서 PATH 추가 후 터미널 재시작
- **macOS 문제** — SSL 오류 → 'Install Certificates.command' 실행 · 명령은 python3 사용

**대본/진행**

[보강 예정]

#### 4. Claude Code 설치 — OS별

**화면**
- Windows (PowerShell): irm https://claude.ai/install.ps1 | iex · 또는 npm i -g @anthropic-ai/claude-code · WSL 없이 네이티브로 · 확인: claude --version
- macOS (Terminal): curl -fsSL https://claude.ai/install.sh | bash · 또는 brew install --cask claude-code · 자동 업데이트 · 확인: claude --version

**대본/진행**

[보강 예정]

#### 5. Claude Code 로그인 & 첫 실행

**화면**
- **실행** — 터미널에서  claude  입력 → 브라우저 OAuth 로그인
- **인증 방식** — Claude 구독 로그인  또는  Console(API 과금)
- **브라우저가 안 열리면** — 터미널에서  c  눌러 URL 복사 → 직접 붙여넣기

**대본/진행**

[보강 예정]

#### 6. Python & Claude Code 설치 실습

**화면**
- 단계: Python 설치 (Win: PATH 체크 / Mac: 인증서 실행) → 확인: python(3) --version · pip(3) → Claude Code 설치 (Win: PowerShell / Mac: curl·brew) → claude 실행 → 로그인 → /help 확인
- 산출물: Python + Claude Code 실행·로그인 완료  문제 시 진단: claude doctor (설치 완료 화면 캡처 삽입)

**대본/진행**

[보강 예정]

### 오후 특강 2 · 클로드 코드 설치 및 주요 설정 (2) — 설정 & 플러그인  (90분 · 14:30–16:00)

*세션 목표: 기본 설정과 CLAUDE.md를 잡고, oh-my-claudecode 플러그인으로 작업 환경을 확장한다.*

#### 1. 무엇을 설정하나

**화면**
- 기본 설정: 모델 · effort · 권한(allow/ask/deny) · CLAUDE.md (프로젝트 규칙) · 슬래시 커맨드
- 플러그인 (확장): oh-my-claudecode · 멀티 에이전트 오케스트레이션 · /plugin install · /autopilot · /team 등

**대본/진행**

[보강 예정]

#### 2. settings.json & 권한

**화면**
- **설정 파일 위치** — ~/.claude/settings.json (전역) · .claude/settings.json (프로젝트)
- **권한 (permissions)** — ask(기본) · allow(미리 허용) · deny(차단) — 예: "Bash(git *)"
- **모델 · 커맨드** — /model 로 모델 변경 · /config · /help · /clear

**대본/진행**

[보강 예정]

#### 3. CLAUDE.md — 프로젝트 규칙

**화면**
- **무엇** — 매 세션 자동 적용되는 프로젝트 규칙·명령어·컨벤션
- **생성** — /init 로 자동 생성한 뒤 편집
- **위치** — 프로젝트 루트 ./CLAUDE.md (전역은 ~/.claude/CLAUDE.md)

**대본/진행**

[보강 예정]

#### 4. oh-my-claudecode 란?

**화면**
- **무엇** — Claude Code용 멀티 에이전트 오케스트레이션 플러그인
- **왜** — 전문 에이전트들을 조율해 복잡한 작업을 자동화 — 'zero learning curve'
- **구성** — 여러 슬래시 커맨드 + 전문 에이전트(executor·planner·reviewer 등)

**대본/진행**

[보강 예정]

#### 5. oh-my-claudecode 설치

**화면**
- **① 마켓플레이스 추가** — /plugin marketplace add https://github.com/Yeachan-Heo/oh-my-claudecode
- **② 설치 & 셋업** — /plugin install oh-my-claudecode  →  /omc-setup
- **사전 준비** — team 기능엔 tmux 필요 (brew/apt install tmux)

**대본/진행**

[보강 예정]

#### 6. 핵심 커맨드 둘러보기

**화면**
- **/autopilot "..."** — 아이디어 → 동작 코드까지 자율 실행
- **/team · /ralph · /ultrawork** — 다중 에이전트 협업 · 완료까지 지속 · 병렬 처리
- **/plan · /ask** — 전략 기획 · 외부 모델(codex·gemini)로 교차검토

**대본/진행**

[보강 예정]

#### 7. 설정 & 플러그인 실습

**화면**
- 단계: 기본 설정 (settings.json · 권한) → /init 로 CLAUDE.md 생성·편집 → oh-my-claudecode 설치 (/plugin → /omc-setup) → 샘플 작업 1회전 (예: /autopilot 간단 작업)
- 산출물: 설정 완료 + 플러그인 설치된 작업 환경  예시: examples/claude-setup (settings.json · CLAUDE.md)

**대본/진행**

[보강 예정]

---

## Day 3 (수) — 실생활 미니 프로젝트 + 리서치 이론·팀 구성

### 오전 특강 · AI Agent 기반 실생활 미니 프로젝트 — 기획 (개인)  (90분 · 10:00–11:30)

*세션 목표: 작고 실용적인 개인 프로젝트를 선정하고, 구현 전에 설계한다.*

#### 1. 왜 기획이 먼저인가

**화면**
- **방향 없는 구현은 헤맨다** — 기획이 절반
- **범위를 좁혀야 끝낼 수 있다** — 한 세션 안에 결과
- **'완성'의 정의를 먼저** — 산출물 합의

**대본/진행**

[보강 예정]

#### 2. 좋은 미니 프로젝트 고르기

**화면**
- **작고 명확하게** — 짧은 시간에 결과가 보이는 범위
- **실생활 문제에서 출발** — 후보 예시 placeholder
- **도구로 실현 가능한가** — 현실성 점검

**대본/진행**

[보강 예정]

#### 3. 문제를 한 문장으로

**화면**
- **누구의 어떤 불편인가** — 문제 진술
- **왜 지금 풀 가치가 있나** — 지금 불편이 크거나 자주 생기나
- **성공하면 무엇이 달라지나** — 전·후 차이를 한 줄로

**대본/진행**

[보강 예정]

#### 4. 기획 시 정할 것

**화면**
- 문제 / 사용자: 해결할 문제 · 누가 쓰나 · 성공 기준 · 사용 맥락
- 범위 / 산출물: 할 것 / 안 할 것 · 필요한 도구 · 결과 형태 · 마감

**대본/진행**

[보강 예정]

#### 5. 개인 프로젝트 기획서 작성

**화면**
- 단계: 문제 한 문장 정의 → 사용자·성공기준 적기 → 범위 정하기 → 산출물 형태 결정
- 산출물: 한 장짜리 기획서  템플릿: examples/mini-project/project-plan-template.md

**대본/진행**

[보강 예정]

### 오후 특강 1 · AI Agent 기반 실생활 미니 프로젝트 — 개발 (개인)  (80분 · 13:00–14:20)

*세션 목표: 기획을 바이브코딩으로 구현하며 도구 사용에 익숙해진다.*

#### 1. 바이브코딩으로 만들기

**화면**
- **의도를 명확히 전달** — 좋은 프롬프트 = 좋은 결과
- **작은 단위로 반복** — 만들고 → 확인하고 → 고치고
- **검증을 습관화** — 동작을 눈으로 확인

**대본/진행**

[보강 예정]

#### 2. 개인 프로젝트 개발 실습

**화면**
- 단계: 기획서 기반으로 시작 → 기능 단위로 구현 요청 → 실행·검증·수정 반복 → 결과 정리
- 산출물: 동작하는 미니 프로젝트  (완성 시 1분 데모)

**대본/진행**

[보강 예정]

#### 3. 주요 학습 내용

**화면**
- **도구의 강점과 한계 체감** — 다음 단계로의 교훈
- **리서치 에이전트로 확장** — 다음 세션 연결

**대본/진행**

[보강 예정]

### 오후 특강 2 · 리서치 프로세스(기존·agentic) + 쟁점 토론 + 팀·주제 설정  (90분 · 14:30–16:00)

*세션 목표: 기존 연구 프로세스와 그 agentic 버전·핵심 쟁점을 함께 본 뒤, 팀과 리서치 주제를 정한다.*

#### 1. 기존 연구 프로세스 (사람)

**화면**
- 분야 파악(전체를 훑기) → 관심 좁히기(세부 주제) → 문헌 선별·검증(선별 → 비판적 읽기) → 가설 수립(gap·보완) → 우선순위·검증(높은 것부터)

**대본/진행**

에이전트로 자동화하기 전에, 사람이 연구를 어떻게 하는지부터 봅시다. 보통 분야 전체를 훑고, 세부 관심을 좁히고, 논문을 선별·검증하고, 개선 가설을 세우고, 우선순위를 매겨 검증하죠. 그런데 현실은 이 그림처럼 한 방향으로 흐르지 않습니다 — 가설을 세우다 보면 어떤 논문이 중요한지가 바뀌고, 검증하다 새 공백이 보여요. 즉 직선이 아니라 반복 루프입니다. 또 '재현'은 비용이 커서, 실제로는 논문 전체를 다시 돌리기보다 주장과 근거를 비판적으로 점검하는 '검증'에 가깝습니다.

#### 2. 각 단계를 agent로 — 5개 질문

**화면**
- **① 분야 파악 / ② 논문 선별** — 맵·taxonomy 만들기 / funnel 랭킹(다양성 포함)
- **③ 논문 학습 / ④ 가설 생성** — 구조화 추출·모순 그래프 / gap·조합 기반
- **⑤ 가설 우선순위** — 스코어링·랭킹 (impact × plausibility ÷ 비용)

**대본/진행**

그럼 이 각 단계를 에이전트로 어떻게 옮길까, 라는 다섯 가지 질문이 자연스럽게 나옵니다. 분야 파악은 다 읽는 게 아니라 리뷰 논문에서 출발해 '지도(맵)'를 만드는 것이고, 논문 선별은 넓게 모은 뒤 깔때기처럼 좁히되 모순·이상치도 일부러 남깁니다. 논문 학습은 '읽기'가 아니라 동일한 틀로 구조화 추출해 모순 그래프를 만드는 것이고요. 가설 도출은 공백·모순·조합에서, 우선순위는 영향력·타당성·비용을 곱하고 나눠 정합니다. 이 다섯 단계가 곧 리서치 에이전트가 실제로 다루는 부분입니다.

#### 3. 가설: '다수 생성' vs '선별'

**화면**
- 다수 생성 (breadth): 가용 데이터/도구로 제약 · 싼 테스트로 거르기 · 다양성↑ · LLM 판단 의존↓
- 사전 선별 (selection): 비싼 테스트 전에 추리기 · LLM 판단에 의존 · 위양성↓ · 놓칠 위험

**대본/진행**

여기서 오늘의 핵심 쟁점입니다. 좋은 가설을 얻는 두 가지 길이 있어요. 하나는 비싼 테스트 전에 '될 만한 것'을 똑똑하게 골라내는 선별 전략, 다른 하나는 일단 많이·다양하게 만들어 놓고 싼 테스트로 거르는 다수 생성 전략입니다. 학생들에게 먼저 물어보세요 — 여러분 주제라면 어디에 무게를 두겠냐고. 한쪽은 LLM의 판단력에 의존하고, 다른 쪽은 데이터·도구로 가설을 제약한 뒤 실측으로 거릅니다. 정답을 바로 주지 말고 잠깐 토론을 유도하세요.

#### 4. 테스트가 싸지면 무게중심이 이동

**화면**
- **agent의 강점/약점** — 생성·싼 테스트 throughput ↑ / 새 가설 품질 판단은 약함
- **권고** — 제약된 다수 생성 → 싼 경험적 필터 → 비싼 검증은 소수만
- **주의** — 다중검정 위양성 · 싼 테스트는 proxy (특히 wet-lab/임상)

**대본/진행**

제 권고는 이렇습니다. 에이전트 덕분에 테스트 비용이 확 싸지는 상황이라면, 무게중심은 '제약된 다수 생성 + 싼 경험적 필터' 쪽으로 기울어야 합니다. 이유는, 새 가설의 품질을 미리 판단하는 건 LLM이 가장 약한 부분이거든요 — 차라리 현실에 근거한 싼 테스트로 거르는 게 더 믿을 만합니다. 단 두 가지를 조심해야 해요. 많이 테스트하면 우연히 '성공처럼 보이는 것'이 늘어나는 다중검정 문제, 그리고 싼 테스트는 보통 진짜 결과의 대리물이라는 점입니다. 특히 신약·임상은 최종 검증이 에이전트로도 싸지지 않으니, '싼 인실리코로 넓게 → 좁히고 → 비싼 실측'의 단계 전략으로 가야 합니다.

#### 5. 프로젝트 형식 + 3세션 스코프

**화면**
- **무엇** — 팀별 '풀 리서치 에이전트' 구현 (자기 관심 주제)
- **스코프** — corpus 기반 · 질문→검색→검증→종합→보고 · Day 4 안에 완주
- **산출물** — 핵심 질문에 답하는 1~2장 인용 보고서

**대본/진행**

이제 프로젝트를 안내합니다. 각 팀은 자기 관심 주제로 '풀 리서치 에이전트'를 만듭니다 — 한 단계만 떼는 게 아니라 전체 파이프라인을요. 단, 내일 세 세션 안에 완주할 수 있도록 스코프를 좁힙니다. 핵심 제한은 'corpus 기반' — 오픈 웹을 긁는 대신, 제공하거나 직접 모은 문서 폴더에서 검색합니다. 산출물은 핵심 질문에 답하는 1~2장짜리 인용 보고서예요. 욕심내지 말고 작게 완주하는 게 목표라는 걸 강조하세요.

#### 6. 리서치 에이전트 파이프라인 (만들 것)

**화면**
- 질문(핵심 질문·분해) → 검색(로컬 corpus) → 검증(출처·교차확인) → 종합(근거→답) → 보고(인용 보고서)

**대본/진행**

여러분이 내일 만들 흐름이 이겁니다 — 질문을 분해하고, 로컬 corpus에서 검색하고, 출처를 교차확인해 검증하고, 근거를 종합해서, 인용이 달린 보고서로 내놓는 것. 오늘 Day 1에서 본 RAG 패턴과 똑같죠. 처음부터 다 만들려 하지 말고, 질문→검색→요약의 최소 버전을 먼저 돌린 다음 검증과 인용을 붙이는 순서로 가면 됩니다. 이 그림을 머릿속에 넣고 기획서를 쓰면 한결 수월합니다.

#### 7. 팀 구성 & 주제·기획

**화면**
- 단계: 팀 편성·역할 분담 → 관심 주제 확정 + 핵심 질문 → 자료(corpus) 확보 계획 → 팀 기획서 작성
- 산출물: 팀 기획서 (주제 · 핵심질문 · corpus · 산출물 · 스코프) 예: 신약/바이오 리서치 등 템플릿: examples/research-agent/research-plan-template.md

**대본/진행**

[실습 안내] 이제 팀을 짜고 기획서를 씁니다. 먼저 팀과 역할을 정하고(검색·정리 / 검증 / 보고 등), 관심 주제와 핵심 질문 한 문장을 확정하세요. 그다음 어떤 문서로 검색할지 corpus 확보 계획을 적습니다 — 직접 모아도 되고 제공 자료를 써도 됩니다. 마지막으로 주제·핵심질문·corpus·산출물·스코프를 한 장 기획서에 정리하세요. 다 쓰면 팀별로 한 줄씩 공유합니다. 내일 아침에는 이 주제를 짧게 발표하고 제가 직접 피드백을 드릴 테니, 오늘은 방향을 분명히 잡는 데 집중하세요.

---

## Day 4 (목) — 팀별 리서치 AI Agent — 기획·구현·실행

### 오전 특강 · 팀별 리서치 AI Agent — 기획 및 구현  (90분 · 10:00–11:30)

*세션 목표: 팀 주제 발표·강사 피드백으로 방향을 확정하고, 리서치 에이전트를 설계·구현한다.*

#### 1. 팀별 주제 발표 → 강사 피드백

**화면**
- **팀별 짧은 발표 (~2분)** — 주제 · 선정 이유 · 핵심 질문
- **강사 직접 피드백** — 스코프 적정? · 핵심질문 명확? · corpus 확보?
- **반영** — 피드백대로 스코프·질문 조정 후 빌드 시작

**대본/진행**

[보강 예정]

#### 2. 3세션 안에 만들 것

**화면**
- **파이프라인** — 질문 → 검색(로컬 corpus) → 검증 → 종합 → 인용 보고서
- **스코프 제한** — corpus 기반 — 오픈 웹 크롤링·벡터DB 불필요
- **완주 기준** — Day 4 끝 = 핵심 질문에 답하는 1~2장 인용 보고서

**대본/진행**

[보강 예정]

#### 3. 팀 에이전트 설계 정리

**화면**
- **주제·핵심 질문 재확인** — Day 3 기획서 기반
- **corpus 확인** — 어떤 문서 폴더에서 검색하나
- **산출물 형태** — 인용 보고서 구조 합의

**대본/진행**

[보강 예정]

#### 4. 최소 동작부터 (MVP)

**화면**
- **가장 단순한 흐름 먼저** — 질문 → 검색 → 요약
- **동작 확인 후 확장** — 검증·인용 추가
- **중간 산출물 자주 확인** — 방향 점검

**대본/진행**

[보강 예정]

#### 5. 구현 ① — 기본 흐름 & 검색(로컬 corpus)

**화면**
- 단계: 기본 흐름(질문→검색→요약) → corpus 폴더 검색 연결 → MVP 1회 동작 확인 → 소규모 테스트
- 산출물: 동작하는 리서치 에이전트 MVP  구조: examples/research-agent 참고

**대본/진행**

[보강 예정]

#### 6. 구현 ② — 검증 & 종합·보고

**화면**
- 단계: 검증 로직(출처·교차확인) → 종합 → 인용 보고서 생성 → 상충·불확실성 표기 → 구성도 정리
- 산출물: 검증·인용 보고서까지 되는 에이전트 v1  구조: examples/research-agent 참고

**대본/진행**

[보강 예정]

### 오후 특강 1 · 팀별 리서치 AI Agent — 실행 및 결과 분석 (1)  (80분 · 13:00–14:20)

*세션 목표: 팀 주제로 1차 실행하고, 결과를 관찰하며 반복 개선한다.*

#### 1. 실행 & 반복 개선

**화면**
- **실제 질문으로 구동** — 팀 주제 투입
- **결과 관찰 → 프롬프트·구조 조정** — iteration 루프
- **실패 사례 기록** — 분석의 재료

**대본/진행**

[보강 예정]

#### 2. 1차 실행 실습

**화면**
- 단계: 팀 주제로 1차 실행 → 출력 점검 → 파라미터·프롬프트 조정 → 재실행 & 비교
- 산출물: 1차 실행 로그 + 결과물  실행 기록: run-log-template.md

**대본/진행**

[보강 예정]

#### 3. 1차 결과 빠른 점검

**화면**
- **정확성·출처는?** — 근거 확인
- **빠진 내용은?** — 커버리지
- **다음 개선 포인트** — (2)세션으로 연결

**대본/진행**

[보강 예정]

### 오후 특강 2 · 팀별 리서치 AI Agent — 실행 및 결과 분석 (2)  (90분 · 14:30–16:00)

*세션 목표: 평가 기준으로 결과를 분석하고, 개선 방향을 도출해 재실행한다.*

#### 1. 결과를 평가한다는 것

**화면**
- **좋다/나쁘다를 근거로** — 감이 아니라 기준
- **무엇을 볼 것인가** — 정확성·완전성·재현성
- **개선으로 연결** — 재실행

**대본/진행**

[보강 예정]

#### 2. 정확성·출처 / 누락·편향

**화면**
- **주장에 근거가 있는가** — 출처 확인·신뢰도
- **빠진 관점·편향은 없나** — 커버리지
- **불확실성은 표기됐나** — 한계 명시

**대본/진행**

[보강 예정]

#### 3. 품질 점검 vs 개선 방향

**화면**
- 품질 점검: 정확성·출처 · 누락·편향 · 재현성 · 일관성
- 개선 방향: 프롬프트 보완 · 검증 강화 · 범위 조정 · 출처 확대

**대본/진행**

[보강 예정]

#### 4. 재실행 & 결과 분석 → 최종 확정

**화면**
- 단계: 개선 항목 반영·재실행 → 강점/약점·한계 표 정리 → 발표 후보 결과 선별 → 최종 결과 확정
- 산출물: 최종 결과물 + 분석 노트(강점·약점·개선점)  에이전트 완주! → 분석: result-analysis-template.md

**대본/진행**

[보강 예정]

---

## Day 5 (금) — 마무리(버퍼) + 보고서·발표

### 오전 특강 · 버퍼 / 보완 · 발표 준비  (90분 · 10:00–11:30)

*세션 목표: 에이전트는 Day 4에 완주 — 미완 부분을 보완하고 발표를 준비한다.*

#### 1. 무엇을 마무리하나

**화면**
- **미완성 팀 우선 보완** — Day 4 못 끝낸 부분부터
- **여유 팀은 개선·심화** — 추가 실험·분석 (선택)
- **핵심 메시지 1개 정하기** — 발표의 중심

**대본/진행**

[보강 예정]

#### 2. 보완 & 발표 준비

**화면**
- 단계: 미완 보완·재실행 → 결과 확정 → 재현성 최종 확인 → 발표용 핵심 결과 선별 → 발표/보고서 준비 시작
- 산출물: 발표 가능한 최종 결과물 + 분석 노트

**대본/진행**

[보강 예정]

#### 3. 발표할 핵심 고르기

**화면**
- **가장 강한 발견 1~2개** — 메시지 중심
- **근거·출처 함께** — 신뢰 확보
- **한계도 솔직하게** — 정직한 보고

**대본/진행**

[보강 예정]

### 오후 특강 1 · 보고서 및 발표자료 — AI Agent 이론 및 수행  (80분 · 13:00–14:20)

*세션 목표: AI 에이전트를 활용해 보고서와 발표자료를 빠르게 만들고 다듬는다.*

#### 1. AI 에이전트로 산출물 만들기

**화면**
- **무엇을** — 보고서·발표 슬라이드
- **왜** — 빠른 초안·반복 수정 — 시간 절약
- **원칙** — 초안은 AI, 판단·검증은 사람

**대본/진행**

[보강 예정]

#### 2. 좋은 보고서·발표의 핵심

**화면**
- **하나의 핵심 메시지** — 무엇을 남길 것인가
- **구조: 문제→접근→결과→의미** — 스토리
- **근거·출처로 뒷받침** — 신뢰

**대본/진행**

[보강 예정]

#### 3. AI로 자료 만드는 흐름

**화면**
- 자료·근거 정리(결과·출처 입력) → 초안 생성(보고서/슬라이드) → 검토·수정(사실·톤·구조) → 완성(최종 다듬기)

**대본/진행**

[보강 예정]

#### 4. 효과적인 프롬프트 — 보고서/슬라이드

**화면**
- **맥락 제공** — 주제·청중·목적
- **형식 지정** — 분량·구조·톤
- **자료 첨부** — 결과·근거를 함께 전달

**대본/진행**

[보강 예정]

#### 5. AI 산출물 검증·다듬기

**화면**
- **사실·출처 확인** — 환각 점검 — 필수
- **논리·구조 점검** — 메시지가 분명한가
- **톤·표현 다듬기** — 팀의 목소리로

**대본/진행**

[보강 예정]

#### 6. AI Agent로 보고서·발표자료 제작

**화면**
- 단계: 결과·근거 정리해 입력 → 보고서 초안 생성 → 발표 슬라이드 초안 생성 → 검토·수정·확정
- 산출물: 발표 슬라이드 + 보고서 초안  보고서 양식: examples/research-agent/output/report-template.md

**대본/진행**

[보강 예정]

### 오후 특강 2 · 팀별 프로젝트 발표  (90분 · 14:30–16:00)

*세션 목표: 팀별로 발표하고, 서로 피드백하며 과정을 마무리한다.*

#### 1. 발표 & 피드백

**화면**
- **팀별 발표** — 시간·순서 placeholder
- **상호 피드백 / Q&A** — 무엇을 배웠나
- **강사 총평** — 잘한 점 · 배운 점 · 다음 단계

**대본/진행**

[보강 예정]

#### 2. 과정 회고 및 향후 계획

**화면**
- **배운 것 돌아보기** — LLM → 에이전트 → 도구 → 프로젝트 → 발표
- **현업·연구에 어떻게 적용할까** — 각자의 다음 프로젝트
- **계속 배우기 위한 자원** — Claude Code 문서 · Anthropic 가이드 · oh-my-claudecode

**대본/진행**

[보강 예정]
